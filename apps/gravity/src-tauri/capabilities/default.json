{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the interface talk to the fathom render thread.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
